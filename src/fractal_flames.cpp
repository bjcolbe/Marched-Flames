#include "fractal_flames.h"

using namespace std;

tuple<vector<Vertex>, vector<Color>> FractalFlames::fractal(tuple<int, int, int> dimensions, vector<float> weights) {
	return {vector<Vertex>(), vector<Color>()};
}